schema_version: ores.compose.v1
project: act-mcp-server.rs
allow_lazy_start: false

services:
  act-mcp-server.rs:
    name: act-mcp-server.rs
    runtime: host
    build:
      - ["cargo", "build", "--bins"]
    command: ["./target/debug/act-mcp-server.rs"]
