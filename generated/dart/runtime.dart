// Generated by flags-2-env from .cli-flags.toml. Do not edit.

import 'dart:io';

final class McpEnvValues {
  const McpEnvValues({required this.actMcpFlagsConfig, required this.allowedHosts, required this.allowedOrigins, required this.logFilter, required this.otelExporterOtlpEndpoint, required this.otelServiceName, required this.port, required this.serverAuthSecret});

  final String? actMcpFlagsConfig;
  final String allowedHosts;
  final String allowedOrigins;
  final String logFilter;
  final String? otelExporterOtlpEndpoint;
  final String? otelServiceName;
  final int port;
  final String? serverAuthSecret;
}

/// Pure: resolve values from an explicit lookup.
McpEnvValues loadFrom(String? Function(String key) lookup) {
  return McpEnvValues(
    actMcpFlagsConfig: _nonEmpty(lookup('ACT_MCP_FLAGS_CONFIG')),
    allowedHosts: _nonEmpty(lookup('MCP_ALLOWED_HOSTS')) ?? 'localhost,localhost:8080,127.0.0.1,127.0.0.1:8080,[::1],[::1]:8080,act-mcp-server,act-mcp-server:8080',
    allowedOrigins: _nonEmpty(lookup('MCP_ALLOWED_ORIGINS')) ?? '',
    logFilter: _nonEmpty(lookup('RUST_LOG')) ?? 'info,act_mcp_server=debug',
    otelExporterOtlpEndpoint: _nonEmpty(lookup('OTEL_EXPORTER_OTLP_ENDPOINT')),
    otelServiceName: _nonEmpty(lookup('OTEL_SERVICE_NAME')),
    port: _parseInt(lookup('PORT'), 8080),
    serverAuthSecret: _nonEmpty(lookup('SERVER_AUTH_SECRET')),
  );
}

/// Resolve values from a map of env keys.
McpEnvValues loadFromMap(Map<String, String> env) {
  return loadFrom((key) => env[key]);
}

/// Effectful: read the process environment.
McpEnvValues loadFromOs() {
  return loadFromMap(Platform.environment);
}

String? _nonEmpty(String? raw) {
  final value = raw?.trim();
  if (value == null || value.isEmpty) {
    return null;
  }
  return value;
}

bool _parseBool(String? raw, bool fallback) {
  switch (raw?.trim()) {
    case '1':
    case 'true':
    case 'TRUE':
    case 'yes':
    case 'YES':
      return true;
    case '0':
    case 'false':
    case 'FALSE':
    case 'no':
    case 'NO':
      return false;
    default:
      return fallback;
  }
}

int _parseInt(String? raw, int fallback) {
  return int.tryParse(raw?.trim() ?? '') ?? fallback;
}

double _parseDouble(String? raw, double fallback) {
  return double.tryParse(raw?.trim() ?? '') ?? fallback;
}

String? _nonempty(String? raw) {
  final value = raw?.trim();
  if (value == null || value.isEmpty) {
    return null;
  }
  return value;
}

String requireEnv(String name, String expectedType, List<String> examples, String? value) {
  final trimmed = _nonempty(value);
  if (trimmed != null) {
    return trimmed;
  }
  throw MissingEnv(name: name, expectedType: expectedType, examples: examples);
}

String? pick(
  List<String> keys,
  List<String> order,
  Map<String, String> shell,
  Map<String, String> dotenv,
  Map<String, String> flags,
  String? fallback,
) {
  for (final source in order) {
    final map = source == 'flags' ? flags : source == 'env_file' ? dotenv : shell;
    for (final key in keys) {
      final value = _nonempty(map[key]);
      if (value != null) {
        return value;
      }
    }
  }
  return _nonempty(fallback);
}

String _unquote(String value) {
  final trimmed = value.trim();
  if (trimmed.length >= 2 &&
      ((trimmed.startsWith('"') && trimmed.endsWith('"')) ||
          (trimmed.startsWith("'") && trimmed.endsWith("'")))) {
    return trimmed.substring(1, trimmed.length - 1);
  }
  return trimmed;
}

Map<String, String> parseDotenv(String text) {
  final out = <String, String>{};
  for (final raw in text.split('\n')) {
    var line = raw.trim();
    if (line.isEmpty || line.startsWith('#')) {
      continue;
    }
    if (line.startsWith('export ')) {
      line = line.substring('export '.length).trim();
    }
    final eq = line.indexOf('=');
    if (eq <= 0) {
      continue;
    }
    final key = line.substring(0, eq).trim();
    if (key.isEmpty) {
      continue;
    }
    out[key] = _unquote(line.substring(eq + 1));
  }
  return out;
}

bool _dotenvEnabled() {
  final value = Platform.environment['FLAGS2ENV_DOTENV'];
  return value != '0' && value != 'false' && value != 'FALSE' && value != 'no' && value != 'NO';
}

Map<String, String> loadDotenvFiles(List<String> files) {
  if (!_dotenvEnabled()) {
    return {};
  }
  return files.fold<Map<String, String>>({}, (acc, path) {
    try {
      acc.addAll(parseDotenv(File(path).readAsStringSync()));
    } catch (_) {}
    return acc;
  });
}
final class MissingEnv implements Exception {
  const MissingEnv({required this.name, required this.expectedType, required this.examples});
  final String name;
  final String expectedType;
  final List<String> examples;
  @override
  String toString() => 'missing required environment variable $name\n  expected type: $expectedType\n  examples: ${examples.join(', ')}';
}

Map<String, String> loadEnvMap(
  Map<String, String> shell,
  Map<String, String> dotenv, [
  Map<String, String> flags = const {},
]) {
  final out = <String, String>{};
  final actMcpFlagsConfig = pick(['ACT_MCP_FLAGS_CONFIG'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (actMcpFlagsConfig != null) out['ACT_MCP_FLAGS_CONFIG'] = actMcpFlagsConfig;
  final allowedHosts = pick(['MCP_ALLOWED_HOSTS'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, 'localhost,localhost:8080,127.0.0.1,127.0.0.1:8080,[::1],[::1]:8080,act-mcp-server,act-mcp-server:8080');
  if (allowedHosts != null) out['MCP_ALLOWED_HOSTS'] = allowedHosts;
  final allowedOrigins = pick(['MCP_ALLOWED_ORIGINS'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, '');
  if (allowedOrigins != null) out['MCP_ALLOWED_ORIGINS'] = allowedOrigins;
  final logFilter = pick(['RUST_LOG'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, 'info,act_mcp_server=debug');
  if (logFilter != null) out['RUST_LOG'] = logFilter;
  final otelExporterOtlpEndpoint = pick(['OTEL_EXPORTER_OTLP_ENDPOINT'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (otelExporterOtlpEndpoint != null) out['OTEL_EXPORTER_OTLP_ENDPOINT'] = otelExporterOtlpEndpoint;
  final otelServiceName = pick(['OTEL_SERVICE_NAME'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (otelServiceName != null) out['OTEL_SERVICE_NAME'] = otelServiceName;
  final port = pick(['PORT'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, '8080');
  if (port != null) out['PORT'] = port;
  final serverAuthSecret = pick(['SERVER_AUTH_SECRET'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (serverAuthSecret != null) out['SERVER_AUTH_SECRET'] = serverAuthSecret;
  return out;
}

const List<String> _dotenvFiles = ['.env'];

Map<String, String> loadEnvMapFromOs() {
  return loadEnvMap(Platform.environment, loadDotenvFiles(_dotenvFiles));
}
